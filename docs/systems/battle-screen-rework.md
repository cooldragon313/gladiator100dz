# 戰鬥畫面大改造（D.28）

> 把 `#battle-overlay` 從現有的「橫排卡片」改為「主畫面風格三欄式」。
> 討論日期：2026-04-19
> 實作對應：`battle.js` + `game.html`（待實作）

---

## 目標

- 戰鬥新畫面三欄（跟主畫面一致的視覺語言）
- 準備 3v3 結構（現在只用 1v1 單 slot）
- 每人獨立 ATB（在自己大頭下面）
- 攻擊動畫作用在大頭貼
- 心情用臉色表現（純視覺不影響數值）

---

## 完整佈局

```
┌─ Battle Overlay · 全屏 ──────────────────────────────────────────────┐
│ ┌─ 玩家屬性 ─┐ ┌─ 中央欄 ─────────────────────────┐ ┌─ 敵方屬性 ─┐ │
│ │ 💚 綠臉    │ │ ═══ 戰鬥時間：02:45 ═══            │ │ 🟣 紫臉    │ │
│ │            │ │ ┌─ Stage · arena00 背景 ────────┐ │ │            │ │
│ │ 六維       │ │ │ ┌── 玩家方（3 slot）──────┐   │ │ │ 六維       │ │
│ │ 派生       │ │ │ │ [🔳A] [🔳你] [🔳B]      │   │ │ │ 派生       │ │
│ │ 裝備       │ │ │ │ ░░░▓   ▓▓▓▓    ░░░▓      │   │ │ │ 裝備       │ │
│ │ 特性       │ │ │ │  ↑ 每人 ATB 在大頭下     │   │ │ │ 特性       │ │
│ │            │ │ │ └────────────────────────┘   │ │ │ │            │ │
│ │ ▼切換秀    │ │ │    「看招！」 ⚡ 「呵！」       │ │ │ │ ▼切換秀    │ │
│ │            │ │ │ ┌── 敵方（3 slot）────────┐   │ │ │ │            │ │
│ │            │ │ │ │ [🔳C] [🔳敵] [🔳D]      │   │ │ │ │            │ │
│ │            │ │ │ │ ░░░    ▓▓▓▓   ▓▓░░       │   │ │ │ │            │ │
│ │            │ │ │ └────────────────────────┘   │ │ │ │            │ │
│ │            │ │ └────────────────────────────────┘ │ │ │            │ │
│ │            │ │ ┌─ 主人觀眾區 ─────────────────┐   │ │ │            │ │
│ │            │ │ │ 👤塔倫 👤阿圖斯 👤侍從       │   │ │ │            │ │
│ │            │ │ └──────────────────────────────┘   │ │ │            │ │
│ │            │ │ ╔═ 攻擊按鈕（4 格）═════════════╗ │ │ │            │ │
│ │            │ │ ║  ⚔ 普攻    ⚡ 技能1             ║ │ │ │            │ │
│ │            │ │ ║  🛡 技能2   ▶ 自動              ║ │ │ │            │ │
│ │            │ │ ╚═════════════════════════════════╝ │ │ │            │ │
│ │            │ │ ┌─ 戰鬥日誌 ──────────────────┐   │ │ │            │ │
│ │            │ │ │ （玩家攻擊鐵臂，命中！）       │   │ │ │            │ │
│ │            │ │ └──────────────────────────────┘   │ │ │            │ │
│ └────────────┘ └────────────────────────────────────┘ └────────────┘ │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 規範

### 左右屬性欄（各 20%）
- 心情臉 💚🔵🟣
- 六維（STR / DEX / CON / AGI / WIL / LUK）
- 派生（ATK / DEF / SPD 等）
- 裝備摘要（武器 / 護甲）
- 特性列表（戰鬥相關的）
- 下方 `▼ 切換秀` 下拉 — 3v3 時可選哪個角色的屬性
- 同一個 NPC 只能在一個 slot（不重複）
- 主人指定的 slot 無法換（鎖定）

### 中央欄（60%）從上到下

1. **戰鬥時間條**（整場計時器，一條）
2. **Stage**（arena00 背景）
   - 玩家方 3 slot（1v1 只用中間）
   - 中間：攻擊視覺 + 對話泡泡
   - 敵方 3 slot
3. **主人觀眾區**（塔倫 / 阿圖斯 / 侍從，依 forcedNPCs）
4. **攻擊按鈕 4 格**
5. **戰鬥日誌**（獨立，不跟主畫面日誌共用）

### 攻擊按鈕四格
| 格 | 內容 |
|---|---|
| 1 | ⚔ 普攻 |
| 2 | ⚡ 技能1（gauge 滿時的招式：瞬刺/血怒/盾牆反擊）|
| 3 | 🛡 技能2（情境技：強架/斬首/饒恕）|
| 4 | ▶ 自動（自動戰鬥 toggle）|

### 每人 slot 設計
```
┌──────────┐
│ [頭像/首字] │
│  名字       │
│  HP: ▓▓▓░  │   ← HP 條
│  ATB: ▓▓░ │   ← ATB 條
└──────────┘
```

### 對話泡泡
攻擊時 stage 中央或 slot 上方跳出：
- 「看招！」「給你嘗嘗！」「呵！」（攻擊）
- 「閃得好！」「該死⋯」「啊啊啊！」（防守）
- 通用詞庫（可未來擴充個性化）

### 攻擊動畫
攻擊方 slot **lunge**（往目標方向撲）+ 目標 slot **shake**（震動）
→ 沿用現有 `bt-lunge-right` / `bt-lunge-left` / `bt-hit-shake` CSS

### 心情臉色
- 💚 綠 = 心情 ≥ 60 / 自信
- 🔵 藍 = 心情 30-60 / 專注
- 🟣 紫 = 心情 < 30 / HP 低 / 狂怒

**純視覺，不影響戰鬥數值**。
敵人也動態變色（血量低變紫）。

### 3v3 邏輯
- Slot 位置：左 / 中 / 右
- 1v1 時：只用中間 slot
- 3v3 時：三個都用
- **強制 slot**（主人指定）不可換
- **自由 slot** 可透過下拉切換

---

## 實作分階段

### Step 1：架構 + 外觀
- 重寫 `#battle-overlay` HTML 結構
- 新 CSS：三欄、arena00 背景、slot 樣式
- 無機制，只確認視覺

### Step 2：機制重新接線
- battle.js `_initUI` 改為填入新結構
- HP bar / ATB bar 每人獨立渲染
- 攻擊動畫作用到對應 slot
- 攻擊按鈕 4 格對應現有技能

### Step 3：對話泡泡 + 臉色 + 下拉
- Speech bubble 系統（類似 `_showBgBubble`）
- 心情 → 臉色映射函式
- 換人下拉（Phase 2 / 3v3 用）

---

## 保留不動

- 戰鬥機制（ATB 計算、gauge、技能、斬首評分）
- `_applyDamage` / 傷害公式
- AI 決策邏輯
- 各種 TB_ENEMIES / TB_WEAPONS / TB_ARMORS 資料

只有 UI 變，機制不變。

---

## 🆕 2026-04-20 微調紀錄（測試後回饋）

### 1. 戰鬥畫面 stage 框拉高（敵方 slot 不被遮）

**位置**：[`game.html:3397` `.battle-v2 .bt-stage`](../../game.html#L3397)

| 參數 | 舊 | 新 |
|---|---|---|
| `min-height` | 280px | **400px** |
| `max-height` | 40vh | **55vh** |

**原因**：原本 40vh 導致底下敵方 slot 被下方 audience / 動作按鈕區遮住。

**調整建議**：若還太擠，`max-height` 可拉到 60-65vh。

---

### 2. 中間空間縮小（玩家 vs 對手間距）

**位置**：[`game.html:3503` `.battle-v2 .bt-stage-mid`](../../game.html#L3503)

| 參數 | 舊 | 新 |
|---|---|---|
| `flex` | 1 | 1（保留）|
| `min-height` | 100px | 100px（保留）|
| `max-height` | 無 | **180px** |

**原因**：`flex:1` 吃光所有中間空間導致玩家對手間距過大。加 `max-height` 限制。

**調整區間**：
- 完全緊湊 → 120px
- 對話空間剛好 → **180px**（現值）
- 寬鬆 → 220px+

---

### 3. 心情 emoji 頭像全面隱藏

**使用者回饋**：綠愛心 / 藍紫臉 emoji 破壞戰鬥感覺。

**位置**：
- [`game.html:3332` `.battle-v2 .bt-portrait-mood`](../../game.html#L3332) — 左右欄 emoji
- [`game.html:3479` `.battle-v2 .bt-slot-face`](../../game.html#L3479) — 中間 stage slot emoji

**改動**：`display: none`

**影響**：
- `battle.js:_updateMoodFaces()` 仍會執行但 DOM 不可見
- 不影響戰鬥邏輯 / 數值計算
- mood color 視覺效果失效（未來換真頭像時恢復）

**未來**：加真 portrait.png 時把 `display: none` 改回 flex/block，換 `textContent` 為 `<img>`。

---

### 4. ATB 速度 ×2

**位置**：[`battle.js:1502` `_startAtbLoop`](../../battle.js#L1502)

| 參數 | 舊 | 新 |
|---|---|---|
| `setInterval` 間隔 | 100ms | **50ms**（×2 速度）|
| `RATING_TICKS.S` | 60 | **120** |
| `RATING_TICKS.A` | 100 | **200** |
| `RATING_TICKS.B` | 120 | **240** |

**原因**：使用者反映戰鬥節奏太慢。

**設計**：tick interval 減半 = 戰鬥 ×2 速度。`RATING_TICKS` 等比例調整 → **實際時間標準不變**（S 評分仍需約 6 秒內結束）。

---

### 5. 劇情戰鬥關斬首面板（`sparring: true`）

**位置**：[`main.js` Day 5 沙洗所有 `Battle.start()` 呼叫](../../main.js)

**原因**：使用者發現 Day 5 索爾戰鬥有斬首選項，跟劇本矛盾（索爾必死由劇本決定，不是玩家斬首）。

**改動**：Day 5 所有 3 個戰鬥（trialSol × 2 + trialVeteran）加 `{ sparring: true }` 選項 → 關閉斬首面板 + 關競技場評分。

**規則**：
- **開放式競技場戰鬥** → 不傳 `sparring` → 有斬首 / 饒恕選項
- **劇情戰鬥**（Day 5 / Day 49 / 其他腳本化戰鬥）→ 傳 `sparring: true` → 關選項

**未來**：Day 49 血戰宴會 / 主人指派的血戰都應該 `sparring: true`。

---

## 維護規則

新增 UI 調整時，寫一條到此文件「微調紀錄」章節，列出：
- 位置（檔案 + 行號）
- 參數舊/新
- 原因
- 調整建議
